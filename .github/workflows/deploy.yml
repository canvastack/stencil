name: Deploy to GitHub Pagesname: Deploy to GitHub Pages



on:on:

  push:  push:

    branches: ['master'] # sesuaikan dengan branch utama Anda    branches: [ "main", "master" ]

  workflow_dispatch:

permissions:

  contents: writepermissions:

  pages: write  contents: read

  id-token: write  pages: write

  id-token: write

jobs:

  build:concurrency:

    runs-on: ubuntu-latest  group: "pages"

    steps:  cancel-in-progress: false

      - name: Checkout

        uses: actions/checkout@v3jobs:

        build:

      - name: Set up Node    runs-on: ubuntu-latest

        uses: actions/setup-node@v3    steps:

        with:      - name: Checkout

          node-version: 18        uses: actions/checkout@v4

          cache: 'npm'

            - name: Setup Node.js

      - name: Install dependencies        uses: actions/setup-node@v4

        run: npm install        with:

                node-version: '18'

      - name: Build          cache: 'npm'

        run: npm run build

            - name: Install dependencies

      - name: Deploy to GitHub Pages        run: npm ci

        uses: JamesIves/github-pages-deploy-action@v4

        with:      - name: Build

          folder: dist # folder hasil build        run: npm run build

          branch: gh-pages # branch tujuan deployment
      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}